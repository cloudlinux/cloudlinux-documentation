import{_ as l,n as i,p as s,a2 as n}from"./framework-85b608a1.js";const t={};function a(o,e){return i(),s("div",null,e[0]||(e[0]=[n(`<h1 id="installation" tabindex="-1"><a class="header-anchor" href="#installation" aria-hidden="true">#</a> Installation</h1><div class="tip custom-block"><p class="custom-block-title"></p><p>For the current release, installation from the ISO images is not available.</p></div><p>To install the CloudLinux Subsystem on an Ubuntu machine:</p><ol><li>Make sure you have Python 3 installed on your system. If not, install it using the following command:</li></ol><div class="language-bash line-numbers-mode" data-ext="sh"><pre class="language-bash"><code>apt install python3
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div></div></div><ol start="2"><li>Download the <code>ubuntu2cloudlinux.py</code> script:</li></ol><div class="language-bash line-numbers-mode" data-ext="sh"><pre class="language-bash"><code>wget https://repo.cloudlinux.com/cloudlinux-ubuntu/sources/cln/ubuntu2cloudlinux.py
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div></div></div><ol start="3"><li>Run the script. Provide a key if your license is key-based:</li></ol><div class="language-bash line-numbers-mode" data-ext="sh"><pre class="language-bash"><code>python3 ubuntu2cloudlinux.py --key=your_key
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div></div></div><p>If you have an IP-based license, pass the <code>--byip</code> option instead:</p><div class="language-bash line-numbers-mode" data-ext="sh"><pre class="language-bash"><code>python3 ubuntu2cloudlinux.py --byip
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div></div></div><p>The script will install the CloudLinux Subsystem on your Ubuntu machine. After the installation is complete, reboot your system.</p><h2 id="ubuntu2cloudlinux-explained" tabindex="-1"><a class="header-anchor" href="#ubuntu2cloudlinux-explained" aria-hidden="true">#</a> Ubuntu2Cloudlinux explained</h2><p>The <code>ubuntu2cloudlinux.py</code> script installs the CloudLinux Subsystem For Ubuntu. It is a simpler script compared to the CLdeploy script for CentOS and AlmaLinux. For example, it doesn&#39;t replace the Linux kernel, only adds a dynamic module to it. Here is a list of actions that the <code>ubuntu2cloudlinux</code> script performs:</p><ul><li>Checks that you are running the latest available version of the <code>ubuntu2cloudlinux.py script</code>, and updates itself if necessary.</li><li>Upgrades all installed packages to the latest versions (unless the <code>--skip-full-update</code> option is passed).</li><li>Registers your system with the CloudLinux Network (CLN) using the provided key or IP address.</li><li>Adds CloudLinux repositories for Ubuntu and a corresponding GPG key to your system.</li><li>Installs a minimum set of CloudLinux OS components: lve, kernel module kmodlve-dkms, lve-utils, lve-stats, alt-python.</li><li>Installs CloudLinux Manager (currently works with cPanel on Ubuntu).</li><li>Installs the mod_hostinglimits Apache module.</li><li>Asks you to manually reboot the system to load the new kernel module.</li></ul>`,15)]))}const d=l(t,[["render",a],["__file","index.html.vue"]]);export{d as default};
