import{_ as e,n as a,p as i,a7 as n}from"./framework-5767e03f.js";const s={},c=n(`<h1 id="apache2nginx-beta" tabindex="-1"><a class="header-anchor" href="#apache2nginx-beta" aria-hidden="true">#</a> Apache2Nginx (BETA)</h1><hr><p>Apache2Nginx sets up NGINX hosting on the server and automatically converts <code>.htaccess</code> files into corresponding NGINX configuration.</p><h2 id="installation" tabindex="-1"><a class="header-anchor" href="#installation" aria-hidden="true">#</a> Installation</h2><p>To use Apache2Nginx, first install the <code>apache2nginx</code> package:</p><div class="language-bash line-numbers-mode" data-ext="sh"><pre class="language-bash"><code>yum install apache2nginx
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div></div></div><h2 id="convert-to-nginx-hosting" tabindex="-1"><a class="header-anchor" href="#convert-to-nginx-hosting" aria-hidden="true">#</a> Convert to NGINX hosting</h2><p>After <code>apache2nginx</code> package is installed, you can switch to NGINX by running:</p><div class="language-bash line-numbers-mode" data-ext="sh"><pre class="language-bash"><code>apache2nginx setup
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div></div></div><p>Note that this may take a while, so we recommend running this command in a <code>tmux</code> or <code>screen</code> session.</p><p>After conversion is complete, no further action is required. The monitoring subsystem, when detecting a change to an <code>.htaccess</code> runs conversions to NGINX configuration. So you can continue using <code>.htaccess</code> files as usual.</p><h2 id="switch-back-to-apache-hosting" tabindex="-1"><a class="header-anchor" href="#switch-back-to-apache-hosting" aria-hidden="true">#</a> Switch back to Apache hosting</h2><p>To switch back to Apache, run:</p><div class="language-bash line-numbers-mode" data-ext="sh"><pre class="language-bash"><code>apache2nginx reset
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div></div></div><p>This command removes NGINX from your system and restores Apache configuration.</p><h2 id="force-use-of-apache-for-a-single-website" tabindex="-1"><a class="header-anchor" href="#force-use-of-apache-for-a-single-website" aria-hidden="true">#</a> Force use of Apache for a single website</h2><p>To force individual website to be served by Apache, run:</p><div class="language-bash line-numbers-mode" data-ext="sh"><pre class="language-bash"><code>apache2nginx proxy -d &lt;domain&gt;
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div></div></div><p>For example, <code>apache2nginx proxy -d example.com</code>.</p><h2 id="disable-forced-use-of-apache-for-a-single-website" tabindex="-1"><a class="header-anchor" href="#disable-forced-use-of-apache-for-a-single-website" aria-hidden="true">#</a> Disable forced use of Apache for a single website</h2><p>To disable forcing individual website to be served by Apache:</p><div class="language-bash line-numbers-mode" data-ext="sh"><pre class="language-bash"><code>apache2nginx unproxy -d &lt;domain&gt;
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div></div></div><p>Note that Apache may still serve the website, if:</p><ul><li>conversion of its <code>.htaccess</code> files fails in any way</li><li>unsupported handler is used. Apache2Nginx only supports PHP-FPM handler</li></ul>`,24),o=[c];function d(t,r){return a(),i("div",null,o)}const l=e(s,[["render",d],["__file","index.html.vue"]]);export{l as default};
